<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Uniterm"
    xmlns:System="clr-namespace:System;assembly=mscorlib" xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="Uniterm.Window1"
    Title="Uniterm" Height="755" Width="1230" WindowStyle="SingleBorderWindow" 
        Visibility="Visible" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded" Background="White" Foreground="{x:Null}"
 >

    <Grid x:Name="stackPanel"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch" RenderTransformOrigin="0.5,0.5"
        >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="165*"/>
            <ColumnDefinition Width="68*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Menu x:Name="menu1" Background="{x:Null}" Grid.Column="0
" Height="20" Grid.ColumnSpan="2">
            <MenuItem Header="Plik." Margin="15, 0, 0, 0">
                <MenuItem Header="Nowy" Click="ehNowyClick"/>
                <MenuItem Header="Zapisz" Click="MenuItem_Click_1"/>
            </MenuItem>
        </Menu>
        <Grid RenderTransformOrigin="0.5,0.5"  UseLayoutRounding="True" Grid.Row="1" Grid.ColumnSpan="2" 
        >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="27*"/>
                <ColumnDefinition Width="129*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ListBox x:Name="lbUniterms" BorderThickness="3" SelectionChanged="ehlbUNitermsSelectionChanged" Width="200" Background="{x:Null}" ScrollViewer.HorizontalScrollBarVisibility="Hidden" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <TextBlock Text="{Binding}" 
                                       TextWrapping="Wrap"
                                       HorizontalAlignment="Stretch" 
                                       VerticalAlignment="Center" 
                                       Width="{Binding RelativeSource={RelativeSource AncestorType=ListBox}, Path=Width}"
                                       />
                        </Grid>


                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Border
                BorderThickness="1" BorderBrush="#FF359598" RenderTransformOrigin="0.5,0.5" Grid.Column="1" Grid.ColumnSpan="3">
                <Border.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleY="-0.444"/>
                        <RotateTransform/>
                        <TranslateTransform Y="-2.992"/>
                    </TransformGroup>
                </Border.RenderTransform>

                <DockPanel

                    Grid.RowSpan="2" x:Name="pic" ClipToBounds="True">

                    <ScrollBar

                         SmallChange="1" Orientation="Horizontal" x:Name="HorScroll" Maximum="{Binding Width, ElementName=cDrawing}"  DockPanel.Dock="Bottom" ViewportSize="{Binding ActualWidth, ElementName=cDrawing, Mode=OneWay}"/>

                    <ScrollBar

                         x:Name="VerScroll" Minimum="0" Maximum="{Binding Height, ElementName=cDrawing}" DockPanel.Dock="Right" ViewportSize="{Binding ActualHeight, ElementName=cDrawing, Mode=OneWay}" />

                    <Border

                        ClipToBounds="True">

                        <local:DrawingCanvas

                            x:Name="cDrawing" ClipToBounds="True" Background="White" Margin="0,0,0,-1.2"/>

                    </Border>

                </DockPanel>

            </Border>
            <StackPanel Grid.Column="4" Width="150" HorizontalAlignment="Center">

                <Button

                    Width="114" x:Name="btnAddSek" Margin="0,10,0,0" Content="Sekwencjonowanie" Click="Button_Click" Height="20"/>

                <Button

                    Width="96" x:Name="btnAddEl" Margin="0,20,0,0" Height="23" Content="Eliminacja" Click="btnAddEl_Click"/>

                <Label

                    Width="67" x:Name="label2" Margin="0,20,0,0" Height="24" Content="Czcionka"/>

                <ComboBox

                    Width="120" SelectionChanged="ehCBFontsChanged" x:Name="cbFonts" Height="23"/>

                <StackPanel

                    Height="37" Orientation="Horizontal">

                    <Label

                        Width="55" VerticalContentAlignment="Center" x:Name="label1" HorizontalContentAlignment="Center" Content="wielkość"/>

                    <ComboBox

                        Width="65" SelectionChanged="ehcbfSizeChanged" x:Name="cbfSize" Height="23">

                        <System:String

    >Kegel</System:String>

                    </ComboBox>

                </StackPanel>

                <Button

                    Width="89" x:Name="btnRedraw" Margin="0,20,0,0" Height="23" Content="Zamiana" Click="btnRedraw_Click"/>

            </StackPanel>
        </Grid>

    </Grid>

</Window>